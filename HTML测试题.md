# HTML 核心知识点测试题

> 检验你的学习成果，共20题（10道判断题 + 10道单选题）

---

## 一、判断题（共10题）

请判断下列说法是否正确。

### 第1题
HTML是一种编程语言，可以用来编写复杂的业务逻辑。

---

### 第2题
`<br>`标签是自闭合标签，不需要结束标签。

---

### 第3题
`<h1>`到`<h6>`六个标题标签中，`<h6>`显示的字体最大。

---

### 第4题
`<div>`是块级元素，独占一行；`<span>`是行内元素，不独占一行。

---

### 第5题
在HTML中，`<img>`标签的`alt`属性用于指定图片无法显示时的替代文字。

---

### 第6题
`<a href="https://example.com" target="_blank">`会在当前窗口打开链接。

---

### 第7题
HTML5中，`<header>`、`<nav>`、`<main>`等语义化标签只是`<div>`的替代品，功能完全相同。

---

### 第8题
`<input type="radio">`用于创建单选按钮，同一组单选按钮需要设置相同的`name`属性。

---

### 第9题
`<meta charset="UTF-8">`的作用是设置网页的字符编码，防止中文乱码。

---

### 第10题
`<ol>`用于创建无序列表，`<ul>`用于创建有序列表。

---

## 二、单选题（共10题）

请从四个选项中选择唯一正确答案。

### 第11题
HTML文档的根元素是哪个标签？

A. `<head>`
B. `<body>`
C. `<html>`
D. `<!DOCTYPE>`

---

### 第12题
以下哪个标签用于定义网页标题（显示在浏览器标签页上）？

A. `<h1>`
B. `<title>`
C. `<header>`
D. `<name>`

---

### 第13题
以下哪个属性用于指定超链接的目标地址？

A. `src`
B. `href`
C. `link`
D. `url`

---

### 第14题
以下代码创建的是什么类型的列表？

```html
<ul>
    <li>苹果</li>
    <li>香蕉</li>
</ul>
```

A. 有序列表
B. 无序列表
C. 定义列表
D. 目录列表

---

### 第15题
以下哪个不是HTML5新增的语义化标签？

A. `<section>`
B. `<article>`
C. `<div>`
D. `<aside>`

---

### 第16题
以下哪个`input`类型用于输入密码（输入内容显示为圆点）？

A. `type="text"`
B. `type="hidden"`
C. `type="password"`
D. `type="secret"`

---

### 第17题
以下哪个标签用于创建下拉选择框？

A. `<input type="select">`
B. `<dropdown>`
C. `<select>`
D. `<option>`

---

### 第18题
以下代码中，`id`和`for`属性的作用是什么？

```html
<label for="email">邮箱：</label>
<input type="email" id="email" name="email">
```

A. 设置元素的样式
B. 将标签与输入框关联，点击标签可以聚焦输入框
C. 设置表单提交时的字段名
D. 没有实际作用

---

### 第19题
一个标准的HTML5文档，正确的`<!DOCTYPE>`声明是？

A. `<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 4.01//EN">`
B. `<!DOCTYPE HTML5>`
C. `<!DOCTYPE html>`
D. `<!DOCTYPE xhtml>`

---

### 第20题
以下关于`<form>`标签的`method`属性，说法正确的是？

A. 只能设置为`GET`，不能设置其他值
B. 常用值为`GET`和`POST`，`GET`会将数据附加在URL后，`POST`会将数据放在请求体中
C. `method`属性是可选的，不写的话表单无法提交
D. `method`用于设置表单的样式风格

---

## 答题卡

| 题号 | 你的答案 | 题号 | 你的答案 |
|------|---------|------|---------|
| 1    |         | 11   |         |
| 2    |         | 12   |         |
| 3    |         | 13   |         |
| 4    |         | 14   |         |
| 5    |         | 15   |         |
| 6    |         | 16   |         |
| 7    |         | 17   |         |
| 8    |         | 18   |         |
| 9    |         | 19   |         |
| 10   |         | 20   |         |

---

*请记录你的答案，然后找我对答案吧！*

